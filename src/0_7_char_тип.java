// # **Тип данных `char` в Java: полное руководство с примерами**  

// Тип `char` в Java используется для хранения **символов Unicode** и занимает **16 бит (2 байта)**.
// В отличие от некоторых других языков (например, C++), где `char` — это 8-битный тип, 
// в Java он поддерживает **кириллицу, иероглифы, эмодзи и другие символы** из таблицы Unicode.  

// ---

// ## **1. Основные характеристики `char`**  

// | Характеристика       | Значение                          |
// |----------------------|-----------------------------------|
// | Размер (биты)        | 16                                |
// | Диапазон значений    | `\u0000` (0) до `\uffff` (65535)  |
// | Класс-обёртка        | `java.lang.Character`             |
// | Значение по умолчанию| `\u0000` (пустой символ)          |

// ### **Пример объявления `char`:**
// ```java
char letterA = 'A';          // Латинская 'A'
char digit = '7';            // Цифра '7'
char cyrillicChar = 'Я';     // Кириллическая 'Я'
char heart = '❤';           // Эмодзи ❤
// ```

// ---

// ## **2. Особенности работы с `char`**  

// ### **🔹 1. Unicode и Escape-последовательности**  
// Java использует Unicode, поэтому символы можно задавать:  
/*
- **Через символ:** `'A'`  
- **Через Unicode-код:** `'\u0041'` (это тоже `'A'`)  
- **Через Escape-последовательности:**  
  - `'\n'` — перенос строки  
  - `'\t'` — табуляция  
  - `'\\'` — обратный слеш  
  - `'\''` — одинарная кавычка  
*/

// **Пример:**  
// ```java
System.out.println('\u03A9'); // Выведет: Ω (греческая омега)
System.out.println('\u2764'); // ❤ (сердце)
// ```

// ### **🔹 2. `char` vs `int` (неявное преобразование)**  
// Каждый `char` соответствует числу в Unicode. Можно преобразовывать:  
// ```java
char letter = 'A';
int unicode = letter; // Автоматически в int (65)
System.out.println(unicode); // 65

int num = 97;
char smallA = (char) num; // Явное приведение: 'a'
System.out.println(smallA); // a
// ```

// ### **🔹 3. Сравнение `char`**  
// Символы сравниваются по их Unicode-значениям:  
// ```java
char a = 'A', b = 'B';
System.out.println(a < b); // true (65 < 66)
// ```

// ### **🔹 4. `char` в строках (`String`)**  
// `String` состоит из `char`, но это разные типы:  
// ```java
char[] chars = {'H', 'e', 'l', 'l', 'o'};
String str = new String(chars); // "Hello"
char firstChar = str.charAt(0); // 'H'
// ```

// ---

// ## **3. Практическое применение `char`**  

// ### **📌 1. Перебор символов в строке**  
// ```java
String text = "Java";
for (int i = 0; i < text.length(); i++) {
    char c = text.charAt(i);
    System.out.println(c);
}
// Вывод:
// J
// a
// v
// a
// ```

// ### **📌 2. Проверка, является ли символ буквой/цифрой**  
// ```java
char c = 'X';
if (Character.isLetter(c)) {
    System.out.println("Это буква!");
}
if (Character.isDigit(c)) {
    System.out.println("Это цифра!");
}
// ```

// ### **📌 3. Преобразование регистра**  
// ```java
char lower = 'a';
char upper = Character.toUpperCase(lower); // 'A'
System.out.println(upper);
// ```

// ### **📌 4. Шифр Цезаря (сдвиг символов)**  
// ```java
char original = 'B';
int shift = 3;
char encrypted = (char) (original + shift); // 'E'
System.out.println(encrypted);
// ```

// ### **📌 5. Работа с символами вне ASCII (кириллица, эмодзи)**  
// ```java
char russianChar = 'Д';
char emoji = '😂';
System.out.println(russianChar + " " + emoji); // Д 😂
// ```

// ---

// ## **4. `char` vs `String`**  

// | Критерий          | `char`                        | `String`                                     |
// |-------------------|-------------------------------|----------------------------------------------|
// | **Тип**           | Примитивный (`char`)          | Объектный (`java.lang.String`)               |
// | **Размер**        | Всегда 16 бит                 | Зависит от длины строки                      |
// | **Изменяемость**  | Неизменяем (как примитив)     | Неизменяем (но можно создавать новые строки) |
// | **Использование** | Одиночные символы             | Текст любой длины                            |

// ---

// ## **5. Выводы**  
// - `char` — **16-битный тип**, хранящий символы Unicode.  
// - Подходит для работы с **буквами, цифрами, спецсимволами, эмодзи**.  
// - Можно преобразовывать в `int` (Unicode-код) и обратно.  
// - **Методы `Character`** (`isLetter`, `toUpperCase` и др.) упрощают работу.  
// - В отличие от C/C++, **`char` в Java не эквивалентен байту** (он шире).  

// ### **Когда использовать `char`?**  
// ✔ Обработка одиночных символов (пароль, шифрование).  
// ✔ Работа с Unicode (кириллица, спецсимволы).  
// ✔ Оптимизация памяти (если нужен один символ, а не строка).  

// **Пример:**  
// ```java
char gender = 'M'; // Лучше, чем String gender = "M";
```

// Используйте `char` там, где нужны символы, а `String` — для текста! 🚀