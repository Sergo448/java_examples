// # **Тип данных `double` в Java: полное руководство с примерами**

// В Java `double` — это 64-битный тип данных с плавающей запятой, 
// который используется для хранения дробных чисел с высокой точностью.
// Он является одним из двух основных вещественных типов (наряду с `float`) и применяется 
// в математических вычислениях, научных расчётах, графике и других областях, где важна точность.

// ---

// ## **1. Основные характеристики `double`**
// | Характеристика       | Значение                          |
// |----------------------|-----------------------------------|
// | Размер (биты)        | 64                                |
// | Диапазон значений    | ±4.9e-324 до ±1.8e+308            |
// | Точность             | ~15-17 значащих цифр              |
// | Класс-обёртка        | `java.lang.Double`                |
// | Значение по умолчанию| `0.0d`                            |

// ### **Пример объявления `double`:**
// ```java
double price = 19.99;
double temperature = -5.75;
double scientificValue = 6.022e23; // 6.022 × 10²³
// ```

// ---

// ## **2. Когда использовать `double` вместо `float`?**
// | Критерий          | `float` (32 бита)                                | `double` (64 бита)                       |
// |-------------------|--------------------------------------------------|------------------------------------------|
// | **Точность**      | ~7 значащих цифр                                 | ~15-17 значащих цифр                     |
// | **Скорость**      | Быстрее на старых CPU                            | Медленнее (но сейчас разница минимальна) |
// | **Память**        | Экономит память                                  | Требует больше памяти                    |
// | **Использование** | Рекомендуется только при строгой экономии памяти | Стандартный выбор для дробных чисел      |

// ### **Пример сравнения `float` и `double`:**
// ```java
// float f = 1.23456789f;    // Округляется до 1.2345679
// double d = 1.23456789;     // Сохраняется точно
// System.out.println(f);     // Вывод: 1.2345679
// System.out.println(d);     // Вывод: 1.23456789
// ```

// ---

// ## **3. Особенности работы с `double`**
// ### **🔹 1. Ошибки округления**
// Из-за двоичного представления чисел с плавающей запятой возможны неточности:
// ```java
double a = 0.1 + 0.2;
System.out.println(a); // 0.30000000000000004 (не 0.3!)
// ```

// **Решение:** Использовать `BigDecimal` для точных расчётов:
// ```java
import java.math.BigDecimal;

BigDecimal b1 = new BigDecimal("0.1");
BigDecimal b2 = new BigDecimal("0.2");
System.out.println(b1.add(b2)); // 0.3 (точно)
// ```

// ### **🔹 2. Сравнение `double`**
// Из-за погрешностей прямое сравнение (`==`) ненадёжно:
// ```java
double x = 1.0 / 3.0;
if (x == 0.3333333333333333) { // Ненадёжно!
    System.out.println("Равно");
} else {
    System.out.println("Не равно"); // Может вывести это!
}
// ```

// **Правильный способ:** Сравнение с допустимой погрешностью (`epsilon`):
// ```java
double epsilon = 0.000001;
if (Math.abs(x - 0.333333) < epsilon) {
    System.out.println("Достаточно близко");
}
// ```

// ### **🔹 3. Бесконечность и `NaN`**
// `double` поддерживает специальные значения:
// ```java
double inf = Double.POSITIVE_INFINITY; // +∞
double negInf = Double.NEGATIVE_INFINITY; // -∞
double nan = Double.NaN; // "Not a Number" (результат 0/0, √-1 и т. д.)

System.out.println(1.0 / 0.0); // Infinity
System.out.println(Math.sqrt(-1)); // NaN
// ```

// **Проверка на `NaN`:**
// ```java
if (Double.isNaN(nan)) {
    System.out.println("Это не число!");
}
// ```

// ---

// ## **4. Примеры использования `double`**
// ### **📌 1. Математические вычисления**
// ```java
double radius = 5.0;
double area = Math.PI * Math.pow(radius, 2);
System.out.println("Площадь круга: " + area);
// ```

// ### **📌 2. Финансовые расчёты (с осторожностью!)**
// ```java
double price = 9.99;
double taxRate = 0.20;
double total = price * (1 + taxRate);
System.out.printf("Итог: %.2f$", total); // Итог: 11.99$
// ```

// ### **📌 3. Научные вычисления**
// ```java
double speedOfLight = 2.998e8; // 2.998 × 10⁸ м/с
double planckConstant = 6.626e-34; // 6.626 × 10⁻³⁴ Дж·с
System.out.println("Энергия фотона: " + (planckConstant * speedOfLight));
// ```

// ---

// ## **5. Преобразования с `double`**
// ### **🔹 Автоматическое приведение (расширение)**
// ```java
int i = 5;
double d = i; // Автоматически в double (5.0)
// ```

// ### **🔹 Явное приведение (сужение)**
// ```java
double d = 3.14;
int i = (int) d; // i = 3 (дробная часть отбрасывается)
// ```

// ### **🔹 Преобразование в строку и обратно**
// ```java
String s = "3.1415";
double d = Double.parseDouble(s); // Из строки в double

double num = 2.718;
String str = Double.toString(num); // Из double в строку
// ```

// ---

// ## **6. Выводы**
// - **`double`** — основной тип для чисел с плавающей запятой в Java.
// - **Имеет высокую точность**, но возможны ошибки округления.
// - **Не подходит для точных финансовых расчётов** (лучше `BigDecimal`).
// - **Поддерживает бесконечность (`Infinity`) и `NaN`**.
// - **Сравнивать числа с плавающей запятой нужно через `epsilon`**.

// Используйте `double` там, где важна точность, но помните о его особенностях! 🚀